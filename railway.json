{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "python -m pip install --no-cache-dir -r backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "bash start.sh"
  }
}
